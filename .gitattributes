# Normalize text files to LF in the repo; Git checks out native EOLs as needed.
* text=auto eol=lf

*.md text
*.yml text
*.yaml text
.editorconfig text
.gitattributes text
LICENSE text

# Shell scripts stay LF; keep executable bit via Git mode in commits.
*.sh text eol=lf
