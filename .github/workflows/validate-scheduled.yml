name: Scheduled validate

# Weekly hygiene: same template checks as CI, without waiting for a PR.
on:
  schedule:
    # Monday 06:17 UTC — offset from top-of-hour to reduce Actions contention
    - cron: "17 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: scheduled-validate
  cancel-in-progress: true

jobs:
  validate:
    name: Validate template
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Run template validator
        run: bash scripts/validate-template.sh

      - name: Run validator tests
        run: bash tests/test_validate_template.sh

  shellcheck-scripts:
    name: Shell syntax check
    timeout-minutes: 5
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Bash -n and ShellCheck on scripts and tests
        run: |
          set -euo pipefail
          bash -n scripts/validate-template.sh
          bash -n scripts/extract-changelog-section.sh
          bash -n tests/test_validate_template.sh
          sudo apt-get update -qq
          sudo apt-get install -y -qq shellcheck
          shellcheck -S warning scripts/validate-template.sh scripts/extract-changelog-section.sh tests/test_validate_template.sh
          echo "shell syntax + shellcheck: OK"
